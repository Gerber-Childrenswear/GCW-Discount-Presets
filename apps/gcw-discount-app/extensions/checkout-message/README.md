# GCW Discount Checkout Message

Displays an applied discount message in checkout when a cart attribute named `gcw_discount_id` is set.

Configuration
- `app_url`: Base URL of the app backend for storefront requests.
